CREATE SCHEMA items;

CREATE TABLE items.item (
  id INT GENERATED BY DEFAULT AS IDENTITY NO NULL PRIMARY KEY,
  published_date DATE
  archived BOOLEAN
  label VARCHAR(255)
  genre VARCHAR(255)
  author VARCHAR(255)
)
